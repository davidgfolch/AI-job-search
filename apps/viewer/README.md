# Legacy Job Viewer (Streamlit)

**NOTE: This is the legacy viewer application. For the modern web interface, see `apps/web`.**

A Streamlit-based user interface to visualize and manage job listings.

## Usage

```bash
poetry install
poetry run streamlit run viewer/main.py
```
